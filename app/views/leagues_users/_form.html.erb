<%= form_for(@leagues_user) do |f| %>
  <% if @leagues_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leagues_user.errors.count, "error") %> prohibited this leagues_user from being saved:</h2>

      <ul>
      <% @leagues_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :league_id %><br>
    <%= f.number_field :league_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
